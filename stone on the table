n = int(input())  # Read the number of stones
stones = input().strip()  # Read the stone arrangement

removals = 0  # Initialize removal counter

for i in range(1, n):  # Start from the second stone
    if stones[i] == stones[i - 1]:  # If adjacent stones are the same
        removals += 1  # Increase removal count

print(removals)
